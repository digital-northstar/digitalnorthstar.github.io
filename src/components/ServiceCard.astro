---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---
<!-- 
<div class="relative border border-northtext/20 p-8 rounded-xl 
            hover:border-northtext/40 transition">
-->
            
<div class="relative p-8 transition">

  <!-- Northstar accent -->
  <div class="absolute -top-3 -left-3 text-northtext/30 text-xs tracking-widest">
    ✦
  </div>

  <h3 class="text-lg font-semibold tracking-wide mb-2">
    {title}
  </h3>

  {subtitle && (
    <p class="text-sm text-northtext/70 mb-4">
      {subtitle}
    </p>
  )}

  <div class="prose prose-sm dark:prose-invert">
    <slot />
  </div>
</div>